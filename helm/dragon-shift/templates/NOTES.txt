Dragon Shift is deployed.

{{- if .Values.ingress.enabled }}
Ingress mode:
  Open:
    http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.host }}
{{- else }}
Service mode:
  Service: {{ include "dragon-shift.app.fullname" . }} on port {{ .Values.service.port }}
{{- end }}

Important:
- The default chart path deploys the Rust-only app-server image with the bundled Dioxus web client.
- HTTP API, WebSocket endpoint, and browser UI share the same origin through one service.
- Keep replicaCount=1 until persistence and distributed realtime coordination are added to the Rust runtime.
- Ensure ALLOWED_ORIGINS and VITE_APP_URL match the public application origin.
